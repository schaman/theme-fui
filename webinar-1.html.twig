<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Loading Bootstrap -->
    <link href="{{ 'css/vendor/bootstrap.min.css' | asset_url }}" rel="stylesheet">

    <!-- Loading Flat UI -->
    <link href="{{ 'css/flat-ui.css' | asset_url }}" rel="stylesheet">

    <link rel="shortcut icon" href="{{ 'img/favicon.ico' | asset_url }}">

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements. All other JS at the end of file. -->
    <!--[if lt IE 9]>
      <script src="{{ 'js/vendor/html5shiv.js' | asset_url }}"></script>
      <script src="{{ 'js/vendor/respond.min.js' | asset_url }}"></script>
    <![endif]-->
    <style type="text/css">
      section {
        padding: 80px 0;
      }
      section.gray {
        background: #fafafa;
        border: solid #f0f0f0 1px;
      }
    </style>
  </head>
  <body>
    <section>
      <div class="container">
        <h1>
          {{ title }}
        </h1>
        <div class="row">
          <div class="col-xs-6 col-sm-7 col-md-8">
            <p class="lead">{{ date }}</p>
            <hr/>
            {{ text | markdown }}
            <hr/>
            <!--p>
              <button type="submit" class="btn btn-inverse btn-lg">Узнать больше</button>
            </p-->
          </div>
          <div class="col-xs-6 col-sm-5 col-md-4">
            <div class="well">
              <form>
                <div class="form-group">
                  <input type="text" name="name" class="form-control input-lg" placeholder="Имя" required="required"/>
                </div>
                <div class="form-group">
                  <input type="text" name="phone" class="form-control input-lg" placeholder="Телефон" required="required"/>
                </div>
                <div class="form-group">
                  <input type="email" name="email" class="form-control input-lg" placeholder="Емейл" required="required"/>
                </div>
                <button type="submit" class="btn btn-primary btn-lg btn-block">Записаться</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="gray">
      <div class="container">
        <div class="row">
          <div class="col-xs-6 col-sm-5 col-md-4 col-lg-3">
            <img src="https://www.dropbox.com/s/x35ea1jlgwnwtbb/smiling.jpg?dl=1" class="img-responsive"/>
          </div>
          <div class="col-xs-6 col-sm-7 col-md-6">
            <h2>Вебинар ведёт</h2>
            <p class="lead">Николай Воробьёв</p>
            <ul>
              <li>Международный тренер и коуч с 12-летним опытом</li>
              <li>Владелец 4 компаний</li>
              <li>Автор 4 книг и 15 тренингов</li>
              <li>Ведущий и тренер в 4 телевизионных и радио шоу об отношениях и личной эффективности</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- jQuery (necessary for Flat UI's JavaScript plugins) -->
    <script src="{{ 'js/vendor/jquery.min.js' | asset_url }}"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="{{ 'js/flat-ui.min.js' | asset_url }}"></script>

  </body>
</html>